import styled from "styled-components";
import Card from ".";

export const StyledVehicleCard = styled(Card)`
  flex-direction: column;
  align-items: flex-start;
`;

export default StyledVehicleCard;
